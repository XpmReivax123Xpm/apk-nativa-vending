<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.VendingKiosk"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".app.KioskCatalogActivity"
            android:exported="false" />
        <activity
            android:name=".app.KioskMachinesActivity"
            android:exported="false" />
        <activity
            android:name=".app.KioskLoginActivity"
            android:exported="false" />
        <activity
            android:name=".app.VendingTesterActivity"
            android:exported="false" />
        <activity
            android:name=".app.VendingCalibratorActivity"
            android:exported="false" />
        <activity
            android:name=".app.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".app.kiosk.KioskDeviceAdminReceiver"
            android:description="@string/app_name"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_receiver" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
